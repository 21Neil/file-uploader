<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <%- include('header') %> 
    <% if (currentUser) { %> 
      <%- include('upload') %>
      <%- include('addFolder') %> 
      <div>
        <a href="/share/create/<%= folderId %>"><button>Share</button></a>
      </div>
    <% } %> 
    <% if (locals.folders) { %>
      <div><%- include('folderItem') %></div>
    <% } %> 
    <% if (locals.files) { %>
      <div>
      <%- include('itemItem') %>
      </div>
    <% } %>
  </body>
</html>
